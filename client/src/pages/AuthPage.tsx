import "./auth.css";
interface Props{isRegistering:boolean;isSubmittingAuth:boolean;authError:string|null;onSubmit:(event:React.FormEvent<HTMLFormElement>)=>void;onToggleMode:()=>void}
export default function AuthPage({isRegistering,isSubmittingAuth,authError,onSubmit,onToggleMode}:Props){return <main className="auth-page"><div className="auth-brand"><div className="auth-logo">S</div><strong>SyncSpace</strong></div><section className="auth-card"><div className="auth-heading"><span>{isRegistering?"GET STARTED":"WELCOME BACK"}</span><h1>{isRegistering?"Create your workspace":"Sign in to SyncSpace"}</h1><p>{isRegistering?"Create an account and start collaborating in real time.":"Continue to your documents, teammates and workspace."}</p></div><form onSubmit={onSubmit}>{isRegistering&&<label>Username<input name="username" minLength={3} maxLength={40} required autoComplete="username" placeholder="Your display name"/></label>}<label>Email<input name="email" type="email" required autoComplete="email" placeholder="you@example.com"/></label><label>Password<input name="password" type="password" minLength={8} required autoComplete={isRegistering?"new-password":"current-password"} placeholder="At least 8 characters"/></label>{authError&&<div className="auth-error" role="alert">⚠ {authError}</div>}<button className="auth-submit" type="submit" disabled={isSubmittingAuth}>{isSubmittingAuth?"Please wait…":isRegistering?"Create account":"Sign in"}</button></form><div className="auth-switch">{isRegistering?"Already have an account?":"New to SyncSpace?"}<button type="button" onClick={onToggleMode}>{isRegistering?"Sign in":"Create an account"}</button></div></section><p className="auth-foot">Real-time collaboration · Offline-ready · AI workspace assistant</p></main>}
